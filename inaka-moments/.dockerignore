# Dependencias y builds (se generan dentro de la imagen)
node_modules
.nuxt
.nitro
.output
.cache
dist

# Entorno — los secretos NUNCA entran en la imagen; se inyectan en runtime
.env
.env.*
!.env.example

# Git / IDE / SO
.git
.gitignore
.idea
.fleet
.DS_Store

# Docs, logs e infra (innecesarios en la imagen)
docs
logs
*.log
README.md
supabase
docker-compose*.yml
docker/DEPLOY.md
docker/traefik-dynamic.yml

# Plugins locales de un solo uso (COPY . . los metería igual sin esto,
# git-untracked o no — ver server/plugins/zz-test-mock-resend.ts)
server/plugins/zz-test-*
